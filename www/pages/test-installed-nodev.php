<div class="alert alert-warning">Dev dependencies are not installed!</div>
<p>To install <code>dev</code> dependencies and re-generate the autoloader, try to run:</p>            
<pre>
$ cd path/to/project
$ php path/to/composer.phar update --dev
</pre>
